# mcp-server-blender Configuration

[blender]
EXECUTABLE = "blender"
EXTERNAL = true  # true = skip headless launch, connect to GUI Blender running bootstrap.py
SOCKET_HOST = "127.0.0.1"
SOCKET_PORT = 9876
HEARTBEAT_INTERVAL_S = 5
RESTART_DELAY_S = 3
MAX_RESTART_ATTEMPTS = 5