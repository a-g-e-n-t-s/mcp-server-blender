name: Bump parent AGENTS repo

on:
  push:
    branches: [main]

jobs:
  test:
    uses: ./.github/workflows/test.yml

  bump:
    needs: test
    uses: a-g-e-n-t-s/AGENTS/.github/workflows/bump-submodule.yml@main
    with:
      submodule-path: mcp-servers/mcp-server-blender
    secrets: inherit
